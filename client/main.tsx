import React from 'react';
import ReactDOM from 'react-dom/client';
import KasappLanding from './KasappLanding';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KasappLanding />
  </React.StrictMode>
);